[project]
name = "puzzle-api"
version = "0.1.0"
description = "AI Visual Puzzle Generator API"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "pydantic"
]

[tool.black]
line-length = 88
target-version = ['py39']

[tool.isort]
profile = "black"
