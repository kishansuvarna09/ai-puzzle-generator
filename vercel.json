{
    "installCommand": "pnpm install",
    "buildCommand": "pnpm build",
    "rewrites": [
        {
            "source": "/generate-puzzle",
            "destination": "/server/main.py"
        },
        {
            "source": "/(.*)",
            "destination": "/client/dist/$1"
        }
    ]
}